console.log('LocalStorage kategorileri:'); const cached = localStorage.getItem('categories_v1.1.0'); if (cached) { const { data } = JSON.parse(cached); console.log('Kategori path örnekleri:'); data.forEach(cat => { console.log(`ID: ${cat.id}, Name: ${cat.name}, Path: ${cat.path}`); if (cat.subcategories) { cat.subcategories.forEach(sub => console.log(`  - Sub: ${sub.name}, Path: ${sub.path}`)); } }); }
